void BackLine() 
{
    int a;
    int b;
    writeSc("Enter value of a : ");
    a = readSc(int);
    writeSc("Enter value of b : ");
    b = readSc(int);
    int c;
    c = a + b;
    writeSc("Value of c = a + b : ");
    writeSc(c);


    return EOM;
}